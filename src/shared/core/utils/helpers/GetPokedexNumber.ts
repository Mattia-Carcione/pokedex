/**
 * Estrae il numero di pokédex dall'id.
 */
export function GetPokedexNumber(pokedexId: number): number {
    // In questo caso, il pokédex nazionale corrisponde direttamente all'id del pokédex
    return pokedexId;
}